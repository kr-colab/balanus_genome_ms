% Master file for the point-by-point response to reviewers.
% Compile this file (it \inputs review-response-commands.tex and
% review-responses.tex). Layout adapted from the mkado-paper.
%
%   pdflatex review-responses-main && pdflatex review-responses-main
%
% The second pass resolves the page/line back-references produced by
% \revref / \llname once the companion manuscript supplies the matching
% \revpoint / \llabel line labels. In this standalone document the reply
% stubs do not call \revref, so a single pass is sufficient.

\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{xcolor}
\usepackage{xspace}
\usepackage{lineno}        % required by review-response-commands.tex
\usepackage{hyperref}
\usepackage{parskip}

\hypersetup{
    colorlinks=true,
    linkcolor=black,
    urlcolor=blue!60!black,
    citecolor=black
}

\input{review-response-commands}

\begin{document}

\input{review-responses}

\end{document}
